version: "3"

services:
  db:
    image: mariadb:10@sha256:7e287b5c2a4ddf38bd50dc4b506032df809000c48cb452d2aff39eaaad3f64c9
    command: --max-allowed-packet=64MB
    restart: unless-stopped
    volumes:
      - /var/volumes/matomo/db:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=${MATOMO_MYSQL_ROOT_PASSWORD}
      - MYSQL_PASSWORD=${MATOMO_MYSQL_PASSWORD}
      - MYSQL_DATABASE=matomo
      - MYSQL_USER=matomo
    networks:
      - matomo

  matomo:
    image: matomo:fpm-alpine@sha256:89ec431d862f0d62933bebcf568d0c4933ec7e2bcab0a13e00f4d7588a6fda9c
    restart: unless-stopped
    depends_on:
      - db
    volumes:
      #      - ./config:/var/www/html/config:rw
      #      - ./logs:/var/www/html/logs
      - /var/volumes/matomo/app:/var/www/html
    environment:
      - MATOMO_DATABASE_HOST=db
      - MATOMO_DATABASE_ADAPTER=mysql
      - MATOMO_DATABASE_TABLES_PREFIX=matomo_
      - MATOMO_DATABASE_USERNAME=matomo
      - MATOMO_DATABASE_DBNAME=matomo
      - MATOMO_DATABASE_PASSWORD=${MATOMO_MYSQL_PASSWORD}
    networks:
      - matomo

  web:
    image: nginx:alpine@sha256:686aac2769fd6e7bab67663fd38750c135b72d993d0bb0a942ab02ef647fc9c3
    restart: unless-stopped
    depends_on:
      - matomo
    volumes:
      - /var/volumes/matomo/app:/var/www/html:ro
      # see https://github.com/matomo-org/matomo-nginx
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik"
      - "traefik.http.routers.matomo.rule=Host(`matomo.rknt.de`)"
      - "traefik.http.routers.matomo.entrypoints=websecure"
      - "traefik.http.routers.matomo.tls.certresolver=myresolver"
    networks:
      - traefik
      - matomo

networks:
  traefik:
    external: true
  matomo:
    internal: true
