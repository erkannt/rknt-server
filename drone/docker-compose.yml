version: '3'

services:
  server:
    image: drone/drone:1@sha256:27e3c7900a6ed3ac8023277a51cb769fe3a86390ce84c33e35f8de3ea08a43dd
    volumes:
      - /var/volumes/drone:/data
    environment: 
      - DRONE_GITHUB_CLIENT_ID=${DRONE_GITHUB_CLIENT_ID}
      - DRONE_GITHUB_CLIENT_SECRET=${DRONE_GITHUB_CLIENT_SECRET}
      - DRONE_RPC_SECRET=${DRONE_RPC_SECRET}
      - DRONE_DATABASE_SECRET=${DRONE_DATABASE_SECRET}
      - DRONE_SERVER_HOST=drone.rknt.de
      - DRONE_SERVER_PROTO=http
      - DRONE_USER_CREATE=username:erkannt,admin:true
      - DRONE_USER_FILTER=erkannt
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.drone.rule=Host(`drone.rknt.de`)"
      - "traefik.http.services.drone.loadbalancer.server.port=80"
      - "traefik.http.routers.drone.entrypoints=websecure"
      - "traefik.http.routers.drone.tls.certresolver=myresolver"
    restart: unless-stopped
  
  runner:
    image: drone/drone-runner-docker:1@sha256:93d165dd676fa764e594463cf1752453fdaeb213eb675c09fd6bd6d37a7973b9
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - DRONE_RPC_PROTO=https
      - DRONE_RPC_HOST=drone.rknt.de
      - DRONE_RPC_SECRET=${DRONE_RPC_SECRET}
      - DRONE_RUNNER_CAPACITY=2
      - DRONE_RUNNER_NAME=rknt.de
    ports:
      - 3000:3000
    restart: unless-stopped

networks:
  traefik:
    external: true
