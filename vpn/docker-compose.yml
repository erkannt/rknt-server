version: "3"

services:
  wireguard-easy:
    image: weejewel/wg-easy@sha256:5866b1d920fc34264af73ae864b14acaf10d129c4cd6acfd0bb517e80bc83348
    environment:
      - WG_HOST=vpn.rknt.de
      - PASSWORD=${WG_PASSWORD}
    volumes:
      - /var/volumes/wg-easy/conf:/etc/wireguard
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.vpn.rule=Host(`vpn.rknt.de`)"
      - "traefik.http.routers.vpn.entrypoints=websecure"
      - "traefik.http.routers.vpn.tls.certresolver=myresolver"
      - "traefik.http.services.vpn.loadbalancer.server.port=51821"
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    sysctls:
      - "net.ipv4.conf.all.src_valid_mark=1"
      - "net.ipv4.ip_forward=1"
    ports:
      - 51820:51820/udp
    networks:
      - traefik
    restart: unless-stopped

networks:
  traefik:
    external: true
